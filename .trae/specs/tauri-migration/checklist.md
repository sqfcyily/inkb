- [x] Tauri 成功在 `apps/web` 目录下初始化，且能通过 `pnpm tauri dev` 启动前端和独立窗口。
- [x] Rust 成功创建并连接到 SQLite 本地数据库，表结构与原 Fastify 后端一致。
- [x] 笔记的增删改查 (CRUD) 在 Tauri 桌面端功能正常，数据能正确写入本地 Markdown 文件及 SQLite 索引。
- [x] 文件监听机制 (notify) 能正确捕捉本地 `.md` 文件的外部修改，并更新数据库。
- [x] Git 同步功能正常，能读取状态、检测冲突，并完成与远程仓库的 Push/Pull。
- [x] URL 和文件的导入解析功能正常工作，PDF 能提取文字并保存为笔记。
- [x] AI 摘要和文本补全功能正常工作，流式文本能顺畅展示在前端界面上。
- [x] 前端成功脱离对本地 31777 端口的依赖，全面切换为 Tauri IPC 通信。
- [x] `apps/server` 已被安全移除，项目无多余依赖。
- [x] Tauri 生产构建 `pnpm tauri build` 成功执行，并生成可执行桌面程序。